一、先直接给结论
你现在这个功能，不要一开始就追求“AI识别”，而应该先做成一个规则 + 光谱曲线匹配 + 质量评分 + 可解释输出的本地识别系统。
因为你当前资源只有：

4 个农药类别
每类已有标准光谱
输入是 jpg/png 图片
明确要求 不要 API
当前处于第一步：先识别是哪一种农药残留
所以最合理、最稳妥、最适合 Flutter/Dart 工程落地的路线是：

第一阶段最优路线
图片预处理 → 光谱曲线提取 → 坐标归一化 → 标准化/去噪 → 与标准光谱库做多指标相似度匹配 → 输出最可能农药类别 + 置信度 + 匹配解释

这套方案的优点：

完全本地可运行
不依赖 API
对小样本更友好
结果可解释
适合后续扩展为工业级系统
比直接上神经网络更稳定
二、你当前项目的正确目标边界
你要先把需求切成三层，不要混在一起：

层 1：当前必须完成的目标
输入一张包含光谱图的 jpg/png，输出它最接近的农药类别（四选一）
候选类别：
吡虫啉
扑虱灵
种衣剂
苄·二氯（你写的是“苄·二氣”，这里建议你核实标准化命名，避免后续数据库字段混乱）
当前阶段输出应包含：
预测类别
置信度分数
与四个标准光谱的相似度排名
关键峰位匹配说明
图像质量是否达标
是否建议“人工复核”
层 2：下一步扩展目标
不仅识别类别，还给出残留强弱趋势/可信区间/疑似混合残留提示
层 3：最终长期目标
形成一个完整的“农药残留图谱智能诊断系统”
包括：
多农药识别
混合残留分析
浓度估计
批次管理
本地数据追溯
模型迭代更新
三、总体技术路线：工业级架构建议
你现在的系统不要写成“一坨识别代码”，而应拆成标准的识别流水线。

1. 推荐总体架构
A. 表现层（Flutter UI）
负责：

图片导入（相册 / 相机 / 文件）
ROI 选区调整
识别进度展示
结果页展示
标准光谱对比图展示
诊断报告导出
B. 应用层（UseCase）
负责：

组织识别流程
调用图像服务
调用光谱分析服务
调用标准光谱库
汇总结果
记录日志
C. 领域层（核心算法）
负责：

曲线提取
去噪
基线校正
重采样
峰值检测
相似度计算
分类决策
置信度评估
D. 数据层
负责：

标准光谱库加载
本地识别记录存储
参数配置存储
算法版本管理
2. Flutter/Dart 工程建议目录
建议你的工程按下面结构拆：

text
下载
复制
lib/
├── app/
│   ├── routes/
│   ├── theme/
│   └── config/
├── features/
│   └── pesticide_detection/
│       ├── presentation/
│       │   ├── pages/
│       │   ├── widgets/
│       │   └── controllers/
│       ├── application/
│       │   ├── usecases/
│       │   └── dto/
│       ├── domain/
│       │   ├── entities/
│       │   ├── services/
│       │   ├── repositories/
│       │   ├── algorithms/
│       │   └── value_objects/
│       └── data/
│           ├── datasources/
│           ├── repositories/
│           ├── models/
│           └── assets/
├── shared/
│   ├── utils/
│   ├── math/
│   ├── image/
│   ├── logging/
│   └── storage/
└── main.dart
四、先明确你的输入到底是什么
这一点非常关键，很多 Agent 会漏掉。

你说的是：
“通过 jpg/png 图片的多光谱信息来诊断”

这里要先区分 3 种情况，不同情况算法完全不同：

情况 A：图片本身就是“光谱曲线图”
比如一张图里有横轴波长、纵轴反射率/吸收率，还有一条曲线。
这种情况下，你要做的是：

从图中提取曲线数据，再和标准曲线做匹配
这也是你目前最接近的情况，因为你已经提到：

“实现真实的光谱数据提取和分析逻辑”
“使用图像处理服务提供的曲线提取功能”
结论：
你当前实际上是在做 “光谱图像转数值曲线” 的识别系统。

情况 B：图片是多通道原始采样图
比如不同波段成像后的原始图像，不是曲线图。
这种情况要做通道分析、伪彩特征提取、区域统计。

结论：
你现在显然还没到这一步，不建议混入当前一期。

情况 C：普通 RGB 拍照图像
如果只是普通 JPG/PNG，没有真实多光谱采集硬件支撑，那么“多光谱信息”其实并不完整，识别可靠性会非常弱。

结论：
如果你现阶段拿到的是“光谱仪输出的图谱截图/导出图”，可行。
如果只是普通手机照片，必须明确限制，否则容易误导结果。

五、第一阶段工业级识别方案：完整流程
下面是你应该落地的标准识别流水线。

步骤 1：输入质量校验（必须做）
目标
防止垃圾图片直接进入识别流程，导致误判。

要检测的项目
图片格式是否支持（jpg/png）
分辨率是否足够
是否存在明显模糊
是否过曝/过暗
是否包含完整曲线区域
横纵轴是否可识别
曲线颜色与背景是否有足够对比
是否存在严重压缩伪影
是否有多条曲线干扰
是否存在裁剪缺失
输出
imageQualityScore
isUsable
qualityWarnings
规则示例
分辨率低于阈值：拒绝识别
曲线区域不足 70%：提示重新上传
模糊度低于阈值：提示重拍
步骤 2：光谱图 ROI 识别与裁剪
目标
从整张图片中找到真正的光谱绘图区。

需要做的事
自动检测绘图区边界
识别横轴与纵轴
检测坐标原点位置
分离坐标轴、网格线、标签文字和曲线
如果自动失败，允许用户手动微调 ROI
工业级建议
必须支持：

自动识别
手动修正
修正结果缓存
输出
plotRegion
axisRegion
curveRegion
步骤 3：曲线提取（核心）
目标
把图片中的光谱曲线变成一组 (x, y) 数值点。

子步骤
图像灰度化 / 转换颜色空间
背景分离
网格线去除
坐标轴文字干扰去除
曲线颜色阈值提取
连通域筛选
单像素骨架化
沿 x 方向扫描提取 y 值
断裂补全
平滑处理
关键要求
输出不能只是像素点，必须映射到真实光谱坐标系
如果一列 x 对应多个 y，选最合理曲线点
对缺失段做插值
输出
text
下载
复制
rawCurvePoints: List<Point<double>>
步骤 4：像素坐标转物理坐标
目标
把图片像素点转换为真实波长 / 强度值。

为什么必须做
否则不同尺寸、不同截图、不同缩放下的曲线不能比较。

需要的映射
横轴：像素 x → 波长 λ
纵轴：像素 y → 反射率/吸光度/强度
做法
识别坐标轴最小值最大值
根据刻度做线性映射
若坐标存在非线性缩放，则做分段映射
输出
text
下载
复制
spectrumPoints: List<SpectrumPoint>
SpectrumPoint { wavelength, intensity }
步骤 5：光谱标准化预处理
这是工业识别的关键，不做这一步，匹配很容易不稳。

你必须实现的预处理模块
5.1 重采样
将所有曲线统一到固定波长步长，例如：

400nm ~ 1000nm
每 1nm 或 2nm 一个点
目的
让标准谱和待测谱对齐比较。

5.2 去噪
建议至少实现两种：

移动平均
Savitzky-Golay 平滑（推荐）
目的
减少图像提取抖动带来的尖刺噪声。

5.3 基线校正
有些图像提取出来会整体漂移。

建议方法：

最小值基线修正
多项式基线拟合修正
5.4 归一化
建议同时支持：

Min-Max 归一化
向量归一化
峰值归一化
工业建议
分类比较时优先使用：

向量归一化
或面积归一化
5.5 一阶导数 / 二阶导数特征（增强识别）
对于形状接近的农药曲线，导数谱很有帮助。

输出
normalizedSpectrum
firstDerivativeSpectrum
secondDerivativeSpectrum
步骤 6：标准光谱库设计
你已经说“更新标准光谱库，添加所有四种农药标准数据”，但还不够工业级。

你需要把标准光谱库做成结构化资产库。

每个农药标准数据应包含
基本信息
农药 ID
中文名
英文名/化学名
别名
类别
版本号
数据来源
建立日期
样本条件说明
光谱信息
原始标准曲线
重采样标准曲线
归一化标准曲线
一阶导数谱
二阶导数谱
特征参数
主峰位置列表
次峰位置列表
峰宽
峰高
峰面积
峰间距
特征波段范围
不稳定波段屏蔽范围
识别规则
允许偏移阈值
最低相似度阈值
与其他农药的差异峰位
易混淆对象列表
标准库文件建议
建议存放为本地 JSON 或者二进制资产。

示例结构：

json
下载
复制
{
  "id": "imidacloprid",
  "nameCn": "吡虫啉",
  "version": "1.0.0",
  "wavelengthRange": [400, 1000],
  "interval": 2,
  "spectrum": [...],
  "normalizedSpectrum": [...],
  "firstDerivative": [...],
  "featurePeaks": [
    {"wavelength": 520, "intensity": 0.81, "type": "main"},
    {"wavelength": 680, "intensity": 0.42, "type": "secondary"}
  ],
  "thresholds": {
    "minCompositeScore": 0.78,
    "peakToleranceNm": 8
  }
}
六、分类识别算法：不要只用一种相似度
工业级识别不能只做一个“欧氏距离最小”。
必须做多指标融合评分。

推荐你实现的 6 类评分指标
1. 皮尔逊相关系数
衡量整体趋势是否一致。

适合：

比较曲线整体形状
优点：

对线性缩放不敏感
2. 余弦相似度
比较向量方向一致性。

适合：

归一化后的曲线比对
3. 欧氏距离 / 曼哈顿距离
衡量点对点差异。

适合：

波形对齐后的差异度量
4. 动态时间规整 DTW（强烈建议）
如果曲线存在轻微横向偏移，DTW 非常有效。

适合：

图片提取导致局部峰位偏移
横轴对齐不完美
工业建议
DTW 非常适合你这个场景。

5. 峰值匹配得分
对关键峰做判定：

主峰是否存在
主峰位置偏差
次峰是否匹配
峰高比例是否一致
峰间距是否合理
这一步对“解释性”非常重要。

6. 导数谱相似度
当原谱很像时，用一阶/二阶导数增强区分。

综合评分公式建议
不要让 Agent 自己想，直接规定：

text
下载
复制
综合得分 =
0.25 * 原谱皮尔逊相关
+ 0.20 * 原谱余弦相似
+ 0.15 * (1 - 归一化欧氏距离)
+ 0.20 * DTW相似度
+ 0.15 * 峰值匹配得分
+ 0.05 * 导数谱相似度
你后续可以调权重。

分类决策规则
输出第一名类别前，必须满足：
综合得分 > 最低识别阈值
第一名与第二名差值 > 最小区分阈值
主峰匹配通过
图像质量达标
否则输出：
“疑似 XXX，但置信度不足”
“无法可靠识别，建议重采或人工复核”
七、必须做“可解释输出”，不要只给类别
工业系统不能只输出“这是吡虫啉”。

你必须输出：

结果页应显示
识别类别
置信度
四类候选排名
与标准谱叠加图
关键峰对比图
匹配理由
风险提示
图像质量评分
匹配理由示例
待测谱在 520nm、684nm 附近存在与吡虫啉标准谱一致的主峰/次峰
与吡虫啉标准谱整体相关系数 0.91
DTW 匹配优于其他三类
与扑虱灵在 610nm 波段存在明显差异，因此排除
风险提示示例
图像存在轻微模糊，识别置信度下降
某些关键波段缺失，建议重新采集
第一名与第二名得分接近，建议人工复核
八、Flutter/Dart 工程级模块拆分
下面是你的 Agent 最需要的“任务拆解”。

模块 1：图像导入与校验模块
负责
选择图片
获取元数据
分辨率检测
模糊/亮度检测
格式合法性判断
输出
ImageInput
ImageQualityReport
模块 2：图像处理服务
负责
ROI 检测
曲线区域提取
网格线清除
曲线骨架提取
像素曲线输出
输出
ExtractedCurveResult
模块 3：坐标映射服务
负责
识别坐标轴范围
像素到波长/强度映射
标准坐标化输出
输出
SpectrumData
模块 4：光谱预处理服务
负责
重采样
平滑
去噪
基线校正
归一化
导数计算
输出
ProcessedSpectrum
模块 5：特征提取服务
负责
峰检测
峰宽/峰高/峰面积
特征波段统计
导数特征
输出
SpectrumFeatureSet
模块 6：标准谱库服务
负责
加载四种农药标准谱
返回指定版本标准数据
提供阈值参数
提供差异峰信息
输出
ReferenceSpectrumLibrary
模块 7：相似度分析服务
负责
Pearson
Cosine
Euclidean
DTW
Peak matching
Composite score
输出
SimilarityReport
模块 8：分类决策服务
负责
四类标准谱逐一比较
排序
判定是否可识别
生成最终标签和置信度
输出
PesticideIdentificationResult
模块 9：结果解释与报告服务
负责
生成解释文本
输出告警
生成 PDF/JSON 报告（后续）
输出
DiagnosisReport
九、建议的数据实体设计
这里是 Agent 很容易遗漏的地方。你要提前规定实体。

1. 原始光谱点
text
下载
复制
SpectrumPoint
- wavelength
- intensity
2. 原始光谱数据
text
下载
复制
SpectrumData
- sourceImagePath
- points
- wavelengthStart
- wavelengthEnd
- interval
- sourceType
- qualityScore
3. 预处理结果
text
下载
复制
ProcessedSpectrum
- rawPoints
- resampledPoints
- smoothedPoints
- baselineCorrectedPoints
- normalizedPoints
- firstDerivative
- secondDerivative
- preprocessLogs
4. 峰特征
text
下载
复制
SpectrumPeak
- wavelength
- intensity
- prominence
- width
- area
- type
5. 光谱特征集
text
下载
复制
SpectrumFeatureSet
- mainPeaks
- secondaryPeaks
- valleys
- bandEnergies
- shapeDescriptors
6. 标准农药谱
text
下载
复制
ReferencePesticideSpectrum
- pesticideId
- pesticideName
- version
- normalizedSpectrum
- derivativeSpectrum
- featureSet
- thresholds
7. 单个候选匹配结果
text
下载
复制
CandidateMatchResult
- pesticideId
- pesticideName
- pearsonScore
- cosineScore
- euclideanScore
- dtwScore
- peakMatchScore
- derivativeScore
- compositeScore
- matchedPeaks
- mismatchReasons
8. 最终识别结果
text
下载
复制
PesticideIdentificationResult
- finalLabel
- confidence
- isReliable
- ranking
- imageQualityScore
- warnings
- explanation
- overlayData
十、算法开发顺序：必须按这个顺序推进
这个顺序是最关键的执行规划。

第 1 阶段：先打通最小闭环
目标：让系统能从图到结果。

任务
导入图片
手动选取 ROI
提取曲线
坐标映射
重采样
与标准谱做 Pearson + Cosine + Euclidean 比较
输出四类排名
验收标准
能稳定输出四类候选排序
至少有基础识别结果页
注意
这一步先不追求完美，重点是打通流程。

第 2 阶段：提升准确率
任务
增加去噪和平滑
增加基线校正
增加峰值检测
增加 DTW
增加综合评分
增加阈值判定
增加“不确定/需复核”输出
验收标准
对同类图像重复识别稳定
第一名和第二名能被合理区分
可解释输出基本完整
第 3 阶段：提升工业可用性
任务
自动 ROI 检测
图像质量评分
图谱叠加可视化
识别日志
参数配置中心
标准谱版本管理
本地报告导出
失败回退机制
验收标准
异常图像不会导致崩溃
结果具有审计可追溯性
识别过程可复现
第 4 阶段：形成可扩展平台
任务
支持更多农药类别
支持混合残留提示
支持样本批量处理
支持本地模型参数更新
支持实验室标定模式
十一、标准谱库建设：你现在最容易忽略的问题
你说有“四个农药残留的光谱图片”，但工业级识别不能只靠单张标准图。

最低要求
每种农药至少应有：

1 条主标准谱
1 条归一化标准谱
关键峰清单
峰容差范围
更专业的要求
每种农药建议采集：

多张重复样本图
不同亮度/对比度条件下的图
不同截图分辨率的图
不同批次采样图
然后构建：

均值标准谱
标准差谱
允许波动带
为什么要这么做
因为图像提取过程会产生波动，单一模板太脆弱。

工业级建议
每个类别建立：

主模板
副模板
平均模板
容差模板
识别时取最优匹配，而不是只比一个标准曲线。

十二、你必须加入“质量门控机制”
否则系统很容易输出看起来很自信但实际上错误的结果。

建议设置三层门控
门控 1：图像质量门控
图片不达标就拒识别。

门控 2：光谱完整性门控
曲线提取长度不足、关键波段缺失就降置信度。

门控 3：分类可信度门控
若第一名和第二名差值太小，则不给最终明确结论。

结果等级建议
A：高可信，可直接输出类别
B：中可信，建议复核
C：低可信，仅给候选
D：不可识别
十三、测试与验证方案
工业级项目最怕“写完代码没法证明有效”。

1. 单元测试
每个核心算法都必须单测：

图像质量评分
曲线提取
重采样
去噪
峰检测
Pearson/Cosine/DTW
综合评分
分类决策
2. 数据集测试
建立本地测试集：

四类农药每类若干样本
正常图
模糊图
裁剪不完整图
有噪声图
不属于四类的干扰图
要测的指标
Top-1 Accuracy
Top-2 Accuracy
拒识准确率
误识率
平均处理时间
3. 回归测试
每次调参数后必须验证：

旧样本准确率是否下降
识别阈值是否变化过大
某一类是否被“偏爱”
4. 鲁棒性测试
模拟：

图片缩放
压缩
亮度变化
对比度变化
曲线轻微平移
十四、性能与工程稳定性要求
Flutter 端做图像处理和数学计算时，需要注意性能。

建议
图像预处理放到 isolate 中执行
大数组计算避免频繁拷贝
标准谱库启动时预加载
中间结果可缓存
识别流程做进度回调
错误要分类处理，不能直接抛 UI
错误分类建议
文件错误
图片质量错误
曲线提取失败
坐标解析失败
标准谱缺失
匹配失败
不可识别
十五、工业级 UI/交互流程建议
页面流程
选择图片页
图像预检页
ROI 调整页
识别处理中页
结果总览页
详细对比页
报告页
结果页必须包含
最终类别
置信度仪表
标准谱叠加曲线
四类候选得分柱状图
关键峰表格
风险提示
重新识别按钮
导出报告按钮
十六、你当前项目最推荐的实施清单
下面我给你一个可直接给 AI Agent 执行的任务清单。

第一批任务：基础骨架
目标
搭建完整模块结构与数据结构

任务
建立 pesticide_detection feature 模块
创建所有 domain entities
创建标准谱库 JSON 结构
创建识别 usecase 骨架
创建结果页 DTO
创建日志/异常类型
第二批任务：曲线提取闭环
目标
实现从图像到数值曲线

任务
图片加载与格式校验
ROI 手动框选功能
曲线提取服务
像素坐标转波长/强度服务
光谱点导出调试页面
曲线叠加调试图
第三批任务：光谱分析闭环
目标
实现从曲线到分类结果

任务
重采样
去噪
归一化
峰值检测
Pearson/Cosine/Euclidean
DTW
综合评分器
候选排序器
最终分类决策器
第四批任务：工业质量控制
目标
让结果可解释、可控、可追踪

任务
图像质量评分
识别置信度分级
不确定性输出
风险提示生成
识别日志记录
参数面板
本地结果缓存
第五批任务：验证体系
目标
证明系统有效

任务
建测试样本目录
建自动化测试脚本
建识别指标统计页
建参数对比实验记录
建标准谱版本管理
十七、你应该给 AI Agent 的“强约束说明”
你的 Agent 理解差、容易漏点，所以你必须用强约束式任务描述。
建议你每次下发任务时都附上下面这种格式。

Agent 执行要求模板
你可以直接复制这段给你的 AI Agent：

任务总原则
不允许跳过数据结构设计
不允许把所有逻辑写进一个 service
不允许省略异常处理
不允许省略日志字段
不允许只输出类别，必须输出置信度和候选排序
不允许直接硬编码到 UI
不允许使用 API
所有核心算法必须可单元测试
所有阈值必须配置化
所有标准谱必须资产化存储
当前要实现的模块
图像质量校验模块
ROI 提取模块
曲线提取模块
光谱预处理模块
标准谱库模块
相似度计算模块
分类决策模块
结果解释模块
每个模块必须包含
输入实体
输出实体
service 接口
service 实现
错误类型
单元测试样例
分类结果必须输出
finalLabel
confidence
candidateRanking
imageQualityScore
warnings
explanation
overlayPlotData
十八、你当前最容易踩的坑
坑 1：把“图片识别”误当成“真实多光谱分析”
如果你的输入只是光谱图截图，那么本质上是图谱曲线匹配，不是直接从原始多光谱传感器数据分析。
这个在产品文案和技术文档里必须说清楚。

坑 2：标准谱只有 1 条图
单模板太脆弱，识别会不稳定。

坑 3：没有坐标校准
如果直接拿像素比较，会很不准。

坑 4：没有拒识机制
系统会“瞎猜”，工业上这是大问题。

坑 5：只做整体曲线相似度
容易把形状接近的农药混淆。
必须加峰值匹配和 DTW。

坑 6：只输出一个结果，不做解释
会让系统看起来不专业，也不利于后续标定。