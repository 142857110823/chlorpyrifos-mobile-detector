# 农药残留检测APP源代码清单

## 项目结构

```
APP开发/
├── lib/               # Dart源代码
│   ├── main.dart      # 应用入口
│   ├── ml/            # 机器学习相关
│   ├── models/        # 数据模型
│   ├── providers/     # 状态管理
│   ├── screens/       # 界面屏幕
│   ├── services/      # 服务类
│   ├── utils/         # 工具类
│   └── widgets/       # UI组件
└── ml_training/       # Python训练脚本
```

---

## 1. 主程序入口

| 文件路径 | 功能描述 |
|---------|----------|
| `lib/main.dart` | 应用程序入口，配置多Provider状态管理，设置路由和主题 |

---

## 2. 机器学习模块 (ml/)

| 文件路径 | 功能描述 |
|---------|----------|
| `lib/ml/ml.dart` | 机器学习模块入口，导出所有机器学习相关类 |
| `lib/ml/tflite.dart` | TensorFlow Lite接口封装 |
| `lib/ml/tflite_stub.dart` | TFLite接口的存根实现（用于测试） |
| `lib/ml/model_manager.dart` | 模型管理，负责模型的加载、更新和管理 |
| `lib/ml/deep_learning_analyzer.dart` | 深度学习分析器，使用TFLite模型进行推理 |
| `lib/ml/enhanced_preprocessor.dart` | 数据预处理器，包括基线校正、平滑滤波等 |
| `lib/ml/feature_engineer.dart` | 特征工程，提取光谱数据的特征向量 |

### 2.1 可解释性分析 (ml/explainability/)

| 文件路径 | 功能描述 |
|---------|----------|
| `lib/ml/explainability/explainability.dart` | 可解释性模块入口 |
| `lib/ml/explainability/explainability_result.dart` | 可解释性分析结果模型 |
| `lib/ml/explainability/feature_importance_analyzer.dart` | 特征重要性分析器 |
| `lib/ml/explainability/model_explainer.dart` | 模型解释器 |
| `lib/ml/explainability/shap_approximator.dart` | SHAP值近似计算器 |

---

## 3. 数据模型模块 (models/)

| 文件路径 | 功能描述 |
|---------|----------|
| `lib/models/models.dart` | 模型模块入口，导出所有模型类 |
| `lib/models/analysis_mode.dart` | 分析模式枚举（导入、模拟、深度学习等） |
| `lib/models/detection_result.dart` | 检测结果模型 |
| `lib/models/detection_result.g.dart` | 检测结果模型的自动生成代码 |
| `lib/models/device_info.dart` | 设备信息模型 |
| `lib/models/device_info.g.dart` | 设备信息模型的自动生成代码 |
| `lib/models/spectral_data.dart` | 光谱数据模型 |
| `lib/models/spectral_data.g.dart` | 光谱数据模型的自动生成代码 |
| `lib/models/spectral_file.dart` | 光谱文件模型 |
| `lib/models/user.dart` | 用户模型 |
| `lib/models/user.g.dart` | 用户模型的自动生成代码 |

---

## 4. 状态管理模块 (providers/)

| 文件路径 | 功能描述 |
|---------|----------|
| `lib/providers/providers.dart` | 提供者模块入口，导出所有提供者类 |
| `lib/providers/app_provider.dart` | 应用状态管理，包括主题、统计信息等 |
| `lib/providers/detection_provider.dart` | 检测状态管理，包括检测过程和结果 |
| `lib/providers/history_provider.dart` | 历史记录状态管理，包括加载、筛选等 |

---

## 5. 界面屏幕模块 (screens/)

| 文件路径 | 功能描述 |
|---------|----------|
| `lib/screens/screens.dart` | 屏幕模块入口，导出所有屏幕组件 |
| `lib/screens/splash_screen.dart` | 启动页，显示品牌Logo和加载状态 |
| `lib/screens/home_screen.dart` | 首页/仪表盘，显示统计信息和快捷操作 |
| `lib/screens/detection_screen.dart` | 检测页面，包括样品信息输入和检测模式选择 |
| `lib/screens/history_screen.dart` | 历史记录页面，显示检测历史和搜索筛选 |
| `lib/screens/device_connection_screen.dart` | 设备连接页面，管理蓝牙设备连接 |
| `lib/screens/settings_screen.dart` | 设置页面，管理应用设置和数据 |

### 5.1 认证屏幕 (screens/auth/)

| 文件路径 | 功能描述 |
|---------|----------|
| `lib/screens/auth/login_screen.dart` | 登录页面 |
| `lib/screens/auth/register_screen.dart` | 注册页面 |

---

## 6. 服务模块 (services/)

| 文件路径 | 功能描述 |
|---------|----------|
| `lib/services/services.dart` | 服务模块入口，导出所有服务类 |
| `lib/services/ai_analysis_service.dart` | AI分析服务，集成深度学习和规则引擎 |
| `lib/services/ai_analysis_service_explainability.dart` | AI分析服务的可解释性扩展 |
| `lib/services/bluetooth_service.dart` | 蓝牙服务，管理设备连接和数据传输 |
| `lib/services/camera_service.dart` | 相机服务，用于拍摄样品照片 |
| `lib/services/data_import_service.dart` | 数据导入服务，支持光谱文件导入 |
| `lib/services/explainability_service.dart` | 可解释性服务，提供AI结果的解释 |
| `lib/services/pdf_report_service.dart` | PDF报告服务，生成检测报告 |
| `lib/services/spectral_file_service.dart` | 光谱文件服务，解析.dx和.spc文件 |
| `lib/services/storage_service.dart` | 存储服务，管理本地数据存储 |
| `lib/services/error_handling_service.dart` | 错误处理服务，全局错误捕获和处理 |
| `lib/services/logging_service.dart` | 日志服务，记录应用运行日志 |
| `lib/services/security_service.dart` | 安全服务，管理数据加密和安全 |
| `lib/services/performance_monitor_service.dart` | 性能监控服务，收集性能数据 |
| `lib/services/export_service.dart` | 导出服务，生成文本报告和分享 |
| `lib/services/model_update_service.dart` | 模型更新服务，检查和更新AI模型 |
| `lib/services/app_update_service.dart` | 应用更新服务，检查应用更新 |
| `lib/services/cloud_service.dart` | 云服务，管理云端同步（基础框架） |

### 6.1 PDF IO实现

| 文件路径 | 功能描述 |
|---------|----------|
| `lib/services/pdf_io_native.dart` | 原生平台的PDF IO实现 |
| `lib/services/pdf_io_stub.dart` | Web平台的PDF IO存根实现 |

---

## 7. 工具模块 (utils/)

| 文件路径 | 功能描述 |
|---------|----------|
| `lib/utils/utils.dart` | 工具模块入口，导出所有工具类 |
| `lib/utils/app_theme.dart` | 应用主题定义，包括浅色和深色主题 |
| `lib/utils/constants.dart` | 常量定义，包括颜色、尺寸、路由等 |
| `lib/utils/helpers.dart` | 辅助函数，包括日期格式化、风险等级转换等 |
| `lib/utils/theme.dart` | 主题相关工具（备用） |

---

## 8. UI组件模块 (widgets/)

| 文件路径 | 功能描述 |
|---------|----------|
| `lib/widgets/widgets.dart` | 组件模块入口，导出所有UI组件 |
| `lib/widgets/common_widgets.dart` | 通用组件，如加载指示器、空状态等 |
| `lib/widgets/spectral_chart.dart` | 光谱数据图表组件 |
| `lib/widgets/result_card.dart` | 检测结果卡片组件 |
| `lib/widgets/device_card.dart` | 设备信息卡片组件 |
| `lib/widgets/risk_radar_chart.dart` | 风险等级雷达图组件 |
| `lib/widgets/pesticide_chart.dart` | 农药残留数据图表组件 |
| `lib/widgets/trend_chart.dart` | 检测趋势图表组件 |

### 8.1 可解释性组件 (widgets/explainability/)

| 文件路径 | 功能描述 |
|---------|----------|
| `lib/widgets/explainability/explainability_widgets.dart` | 可解释性组件入口 |
| `lib/widgets/explainability/feature_importance_chart.dart` | 特征重要性图表组件 |
| `lib/widgets/explainability/shap_waterfall_chart.dart` | SHAP瀑布图组件 |
| `lib/widgets/explainability/spectral_highlight_chart.dart` | 光谱高亮图组件 |

---

## 9. Python训练脚本 (ml_training/)

| 文件路径 | 功能描述 |
|---------|----------|
| `ml_training/train_pesticide_model.py` | 农药残留检测模型训练脚本，包括数据生成、模型训练和TFLite转换 |
| `ml_training/generate_tflite.py` | TFLite模型生成脚本 |
| `ml_training/requirements.txt` | Python依赖包清单 |

---

## 10. 配置文件

| 文件路径 | 功能描述 |
|---------|----------|
| `pubspec.yaml` | Flutter项目配置文件，定义依赖包和项目信息 |

---

## 代码文件统计

| 模块 | 文件数 |
|------|--------|
| 主程序入口 | 1 |
| 机器学习模块 | 15 |
| 数据模型模块 | 11 |
| 状态管理模块 | 4 |
| 界面屏幕模块 | 9 |
| 服务模块 | 19 |
| 工具模块 | 5 |
| UI组件模块 | 12 |
| Python训练脚本 | 3 |
| 配置文件 | 1 |
| **总计** | **80** |

---

## 功能模块对应关系

| 功能模块 | 对应源代码文件 |
|---------|---------------|
| 蓝牙通信 | `services/bluetooth_service.dart` |
| AI检测分析 | `services/ai_analysis_service.dart`, `ml/deep_learning_analyzer.dart` |
| 数据存储 | `services/storage_service.dart` |
| PDF报告 | `services/pdf_report_service.dart` |
| 数据导入 | `services/data_import_service.dart`, `services/spectral_file_service.dart` |
| 相机服务 | `services/camera_service.dart` |
| 用户界面 | `screens/`目录下所有文件 |
| 状态管理 | `providers/`目录下所有文件 |
| 工具辅助 | `utils/`目录下所有文件 |
| UI组件 | `widgets/`目录下所有文件 |
| 模型训练 | `ml_training/train_pesticide_model.py` |

---

**说明**：本清单基于项目当前状态生成，包含所有源代码文件及其功能描述。文件路径均为相对路径，以`APP开发`目录为根目录。

**生成时间**：2026年04月09日
**生成工具**：AI Assistant
